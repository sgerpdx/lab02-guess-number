

// This file is obsolete as it was accidentally created in the test folder -- this function has been moved to domUtils.js

const function guessEval(input, target) {

    if (input > target) {
        return 1;
    } else if (input < target) {
        return -1;
    } else
        return 0;
}